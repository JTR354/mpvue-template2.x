---
to: "src/pages/<%= h.inflection.dasherize(packageName) %>/<%= h.inflection.dasherize(name) %>/modules/<%= h.inflection.dasherize(name) %>.js"
---
export const state = {
  todo: true
}

export const getters = {}

export const mutations = {}

export const actions = {}
